{
  "Logging": {
    "LogLevel": {
      "Default": "Information",
      "Microsoft": "Warning",
      "Microsoft.Hosting.Lifetime": "Information"
    }
  },
  "AllowedHosts": "*",
  "DATABASE_URL": "server=localhost; port=5432; database=postgres; user id=postgres; password=password"
  //docker run -d -p 5432:5432 --name postgres -e POSTGRES_PASSWORD=password --restart always postgres
}
